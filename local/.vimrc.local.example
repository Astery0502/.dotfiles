" ~/.vimrc.local — machine-specific vim config (not tracked by git)
" Copy this file to ~/.vimrc.local and edit as needed.

" --- Local plugin paths ---
" set rtp+=/path/to/local/plugin

" --- Machine-specific settings ---
" set guifont=Menlo:h14

" --- Color scheme overrides ---
" colorscheme desert
