# ~/.bashrc.local — machine-specific bash config (not tracked by git)
# Copy this file to ~/.bashrc.local and edit as needed.

# --- Conda ---
# Uncomment and adjust the path to your conda installation:
# __conda_setup="$('$HOME/miniconda3/bin/conda' 'shell.bash' 'hook' 2> /dev/null)"
# if [ $? -eq 0 ]; then
#     eval "$__conda_setup"
# else
#     if [ -f "$HOME/miniconda3/etc/profile.d/conda.sh" ]; then
#         . "$HOME/miniconda3/etc/profile.d/conda.sh"
#     else
#         export PATH="$HOME/miniconda3/bin:$PATH"
#     fi
# fi
# unset __conda_setup

# --- Project paths ---
# export AMRVAC_DIR=$HOME/science/AGILE-experimental
# export PATH=$PATH:.:$AMRVAC_DIR:$AMRVAC_DIR/tools
# export AM_HOME="$HOME/Documents/os-jyy/os-workbench/abstract-machine"
# export TLIB_PATH="$HOME/Documents/os-jyy/demo/concurrency/thread-lib"

# --- Additional PATH ---
# export PATH="$HOME/Downloads/usr/local/bin/:$PATH"

# --- Claude Code auth ---
# Set this to skip browser login on new machines.
# Get your key from: https://console.anthropic.com/settings/keys
# export ANTHROPIC_API_KEY="sk-ant-..."

# --- Secrets ---
# Source private environment variables (API keys, tokens, etc.)
if [ -f ~/.config/secrets/packy.env ]; then
    source ~/.config/secrets/packy.env
fi

# --- Homebrew (macOS) ---
# eval "$(/usr/local/bin/brew shellenv)"
# eval "$(/opt/homebrew/bin/brew shellenv)"
